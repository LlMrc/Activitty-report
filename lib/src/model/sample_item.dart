/// A placeholder class that represents an entity or model.
class SampleItem {
  const SampleItem(this.id, this.imagePath, this.title);
   final String imagePath; // Add image path field
   final String title;
  final int id;
}
