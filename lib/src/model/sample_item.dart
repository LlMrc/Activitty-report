/// A placeholder class that represents an entity or model.
class SampleItem {
  const SampleItem(this.id, this.imagePath, this.title, this.routeName);
  final String imagePath; // Add image path field
  final String title;
  final String routeName;
  final int id;
}
